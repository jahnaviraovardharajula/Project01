const express=require('express');const cors=require('cors');const app=express();app.use(cors());app.use(express.json());app.post('/api/submit',(req,res)=>{const{cardNumber,expiryDate,cvv,zip}=req.body;if(!cardNumber||!expiryDate||!cvv||!zip)return res.status(400).json({error:'Missing fields'});const token='tok_'+Math.random().toString(36).slice(2,12);res.json({token,last4:cardNumber.slice(-4)})});app.listen(3000,()=>console.log('Backend running on http://localhost:3000'));